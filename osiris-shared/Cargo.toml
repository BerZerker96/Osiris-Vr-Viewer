[package]
name = "osiris-shared"
version = "0.5.0-dev1"
edition = "2021"
description = "Types shared between the Osiris VR Viewer runtime and its GUI."
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bytemuck = { version = "1.13", features = ["derive"] }
